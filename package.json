{
  "name": "ha-home-energy-advisor",
  "version": "0.0.1",
  "private": true,
  "description": "Local commit tooling for the Home Energy Advisor integration. The integration itself is Python; Node is here only for husky and commitlint.",
  "license": "MIT",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^21.2.0",
    "husky": "^9.1.7",
    "sonarqube-scanner": "^5.0.0"
  }
}
