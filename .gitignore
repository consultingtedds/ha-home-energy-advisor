# Python
__pycache__/
*.py[cod]
.venv/
venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
coverage.xml
coverage-frontend/
htmlcov/
dist/
build/
*.egg-info/

# Node (commitlint/husky tooling)
node_modules/

# SonarQube scanner working directory and the CLI pysonar-scanner downloads
.scannerwork/
.scanner/

# IDE
.idea/
.vscode/

# OS
.DS_Store
Thumbs.db

# Real-instance captured data - never committed. Whole-house readings are an
# occupancy trace, and this is a public repo (HEA-19, HEA-63). Tests that use a
# capture must skip when it is absent, so CI stays green without it; anything CI
# needs to prove must be reproduced by a synthetic fixture instead.
tests/fixtures/exploration_2026_07/
tests/fixtures/bad_source_capture/
